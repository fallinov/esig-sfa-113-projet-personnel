---
export interface Props {
  number: number;
  title: string;
  description: string;
}

const { number, title, description } = Astro.props;
---

<div class="objectif-item">
    <span class="objectif-number">{number}</span>
    <h3>{title}</h3>
    <p>{description}</p>
</div>
